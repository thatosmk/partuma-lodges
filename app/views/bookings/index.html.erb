<div class="container-fluid">
    <div class="row">
        <div class="col-sm-9 col-md-2 col-lg-2 sidebar">
            <%= render 'site/sidebar' %> 
        </div>
        <div class="col-sm-3 col-md-10 col-lg-10">
            <p id="notice"><%= notice %></p>
            <div class="row">
                <%= link_to 'New Booking', rooms_path, class: "btn btn-success btn-lg" %>
            </div>
            <div class="row">
                <% @bookings.each do |booking| %>
                <% end %>
            </div>
        </div>
    </div>
</div>

<br>

